name: SASjs Server Executable Release

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-22.04

    strategy:
      matrix:
        node-version: [lts/*]

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install Dependencies WEB
        working-directory: ./web
        run: npm ci

      - name: Build WEB
        working-directory: ./web
        run: npm run build
        env:
          CI: true

      - name: Install Dependencies API
        working-directory: ./api
        run: npm ci

      - name: Build Executables
        working-directory: ./api
        run: npm run exe
        env:
          CI: true

      - name: Compress Executables
        working-directory: ./executables
        run: |
          zip linux.zip api-linux
          zip macos.zip api-macos
          zip windows.zip api-win.exe

      - name: Generate SHA256 checksums
        working-directory: ./executables
        run: |
          sha256sum linux.zip macos.zip windows.zip > SHA256SUMS
          cat SHA256SUMS

      - name: Install Semantic Release and plugins
        run: |
          npm i
          npm i -g semantic-release

      - name: Release
        run: |
          GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }} semantic-release

      - name: Document the checksums on the release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          # semantic-release creates the tag locally when it publishes a
          # release, so an empty result means this push produced no release.
          TAG=$(git tag --points-at HEAD | head -n1)
          if [ -z "$TAG" ]; then
            echo "No release was created for this commit - skipping."
            exit 0
          fi
          bash .github/scripts/add-verify-section.sh "$TAG"
